{
  "extends": ["next/core-web-vitals", "next/typescript"],
  "rules": {
    "@typescript-eslint/no-unused-vars": "off",   // Disables the rule for unused variables
    "prefer-const": "off",                        // Disables the prefer-const rule
    "@typescript-eslint/no-explicit-any": "off"   // Disables the rule for no-explicit-any
  }
}
